export default function Action({ actionable, text, action }) {
  return <div className="game-component">TODO: Actions</div>;
}

// start, cut, flip, deal, send to crib, play, go, score, (hand/crib), ok (aprove score)
