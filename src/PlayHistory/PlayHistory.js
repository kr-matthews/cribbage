export default function PlayHistory({ messages }) {
  return <div className="game-component">TODO: Play History</div>;
}

// messages is an array of { type, colour, text }s
//  type: "auto" or "custom"
//  text is to be displayed as is, with given colour
