export default function Header({
  userName,
  setUserName,
  mode,
  setMode,
  isSoundOn,
  toggleSound,
  code,
  create,
  join,
  leave,
  players,
  dealer,
}) {
  return <div className="game-component">TODO: Header</div>;
}
